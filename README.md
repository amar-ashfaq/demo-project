# Demo Project

This is a simple demo project to showcase how to use Git, GitHub, and Visual Studio for development.

## Setup

1. Clone the repository.
2. Run `dotnet build` to build the project.
3. Run `dotnet run` to execute it.
